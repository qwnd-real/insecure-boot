[package]
name = "uacpi-sys"
description = "Raw bindings to the uACPI ACPI implementation, built for the UEFI ABI."
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
links = "uacpi"

[lib]
test = false
bench = false

[build-dependencies]
bindgen.workspace = true
cc.workspace = true
